# web + server
fastapi
uvicorn[standard]

# Database support
aiosqlite  # For async SQLite support
motor      # For MongoDB support
pymongo[srv]
python-dotenv
pydantic[email]
pydantic-settings

# ODM (Optional, but recommended)
beanie

# data science / ML
pandas
numpy
scikit-learn
xgboost-cpu       # CPU-only XGBoost (smaller / easier). Use xgboost if you prefer standard package.
prophet           # optional heavy dependency for baseline forecasting

# model utils
joblib
mlflow            # optional - model registry

# testing / dev
pytest
pytest-asyncio
httpx
